# packages/mobile

Expo React Native skeleton demonstrating use of the shared-crypto package. This is a minimal demo — for production the shared-crypto implementation must be replaced with a libsodium/Argon2-backed implementation suitable for React Native (native modules or FFI).
